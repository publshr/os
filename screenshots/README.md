# NOVA boot screenshots

Auto-captured in CI by booting the ISO in QEMU (UEFI + BIOS) at intervals.
The `uefi-045s.png` / `uefi-300s.png` frames show NOVA booting to the branded installer.
(Frames named *-400s+ may be blank — QEMU had powered down by then.)
